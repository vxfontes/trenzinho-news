import { Grid, styled } from "@mui/material";

export const GridStyled = styled(Grid)({
    backgroundColor: '#ffffff',
    borderRadius: 40,
    display: 'block',
    padding: 30,
    paddingBottom: 40
})