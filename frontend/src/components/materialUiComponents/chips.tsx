import { Chip } from "@mui/material";
import { styled } from "styled-components";

export const CustomChipYellow = styled(Chip)({
    backgroundColor: '#ff842d',
    color: '#fff',
});

export const CustomChipGreen = styled(Chip)({
    backgroundColor: '#3fb1b5',
    color: '#fff',
});

export const CustomChipBlue = styled(Chip)({
    backgroundColor: '#5a97f7',
    color: '#fff',
});

export const CustomChipRed = styled(Chip)({
    backgroundColor: '#fe5244',
    color: '#fff',
});

export const CustomChipPurple = styled(Chip)({
    backgroundColor: '#d258ac',
    color: '#fff',
});